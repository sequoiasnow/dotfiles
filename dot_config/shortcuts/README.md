# Apple Shortcuts

A place to save apple shortcuts that are actually useful.

Shortcuts are frustrating as an interface, but they integrate exceedingly
well with MacOS and can be good harnesses between GUI widgets and scripts.

Generally, unless a shortcut is using some particularly useful built-in
capability the shortcut should simply execute a shell script so the same
commands are accessible via each pathway.
